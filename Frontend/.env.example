# Vite env vars — copy to .env.local for local dev
# Do NOT commit real values. This file is safe to commit (no secrets).

# Leave empty in local dev; the vite.config.js proxy handles /api --> localhost:8000
VITE_API_BASE_URL=
